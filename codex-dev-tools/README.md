# codex-dev-tools
Docker images to be used for local development or integration testing purposes.

## How to Use
From the terminal, `cd` into the directory for the tool you need and then run `docker-compose up`.